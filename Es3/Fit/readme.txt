The program expFit.cc is a standalone C++ program that uses the ROOT
class TMinuit to carry out a Maximum Likelihood fit.  To build the
program at RHUL, download all the files to a separate directory and
type:
 
gmake

Good luck,
 
GC
